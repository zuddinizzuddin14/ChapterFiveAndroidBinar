<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.mymovieapp">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyMovieApp.NoActionBar"
        tools:targetApi="31">
        <activity
            android:name=".presentation.ui.profileuser.changepassword.ChangePasswordActivity"
            android:exported="false" />
        <activity
            android:name=".presentation.ui.registeruser.RegisterActivity"
            android:exported="false" />
        <activity
            android:name=".presentation.ui.loginuser.LoginActivity"
            android:exported="false" />
        <activity
            android:name=".presentation.ui.profileuser.ProfileActivity"
            android:exported="false" />
        <activity
            android:name=".presentation.ui.detailmovie.DetailMovieActivity"
            android:exported="false" />
        <activity
            android:name=".presentation.ui.searchmovie.MovieSearchActivity"
            android:exported="false"
            android:theme="@style/Theme.MyMovieApp" />
        <activity
            android:name=".presentation.ui.listmovie.MovieListActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>